# Payment API Sample

Public-safe source fixture for the deterministic payment-api review payload.

The source-first pipeline uses this small project to show that the public sample
review is based on sanitized code/config context, not only logs. It models a
Cloud Run checkout service with a database connection pool, payment gateway
timeouts, deployment metadata, and read-only collectors.
